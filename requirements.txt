# Phase 1 - ingest
# Local stdlib-only; boto3 needed when BUCKET is set (S3 / Lambda).
boto3>=1.34

# Phase 2 - silver
pandas>=2.0
pyarrow>=14.0

# Phase 6 - reverse DCF (root-finding)
scipy>=1.11

# Live market data for DCF anchor price
yfinance>=0.2.30

# Phase 5 - visualization
matplotlib>=3.7
